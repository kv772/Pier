specVersion: "v1beta"
serviceId: "back-end"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "back-end-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
